import './search-panel.css';

const SearchPanel = () => {
    return (
        <input
            type="text"
            className="form__field"
            placeholder="Search employee.." />
    );
}

export default SearchPanel;